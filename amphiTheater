#GLOBAL VALUES
pi = 3.14
halfPi = pi / 2

function seats(cX cY rowSize ) {
  for i <- 3 to 22 {
    multiplied = i * rowSize
    startAngle = 2
    endAngle = 3   
    arc(cX cY multiplied startAngle endAngle )
  }
}

# centerX, centerY, rowSize, radial
seats(0 0 20 90)