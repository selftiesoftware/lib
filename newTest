import geometry2d

rectangle(-90 0 90 12)

def scale = 500000
def altScaled as Number = 1000 * 305

#draw glide distance for the given altitude
def glide(ratio as Number alt as Number) = {
  def xValue = ratio * alt
  line(0 0 xValue alt)
}



glide(10 altScaled / scale)