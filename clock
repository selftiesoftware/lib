#CLOCK DIAMETER
def d = 157

#FIVE MIN MARKS SIZE
def fiveSize = 8

#ONE MINUTE MARKS SIZE
def oneSie = 4

#SIZE OF THE DIGITS
def textSize = 11
#INWARDS OFFSET OF DIGITS
def innerText = 10

#ADJUSTMENT OF TEXT
def textOffsetX = 2
def textOffsetY = 2.5

#MARGIN BETWEEN PERIPHERY AND MARKS
def outer = (d / 2) - 1
def innerFive = (d / 2) - 5
def innerOne = (d / 2) - 3

#FIVE MINUTE MARKS
repeat -2 to 10 using i {
def x = -1 * cos(radians(i * 30))
def y = sin(radians(i * 30))

#TEXT
def textX = (-1 - x * (d / 2) - innerText) - textOffsetX
def textY = (-1 - y * (d / 2) - innerText) - textOffsetY
text(textX textY textSize i + 3)
line(x * innerFive y * innerFive x * outer y * outer)
}

#ONE MINUTE MARKS
repeat 0 to 60 using i {
def x = cos(radians(i * 6))
def y = sin(radians(i * 6))
line(x * innerOne y * innerOne x * outer y * outer)
}

#OUTER AND CENTER CIRCLE
circle(0 0 d / 2)
circle(0 0 1)
